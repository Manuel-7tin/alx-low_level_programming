#include <stdio.h>
#include <stdlib.h>

/**
 * add - adds positive numbers.
 *
 * @a: first number
 * @b: second number
 *
 * Return: 0
 */

int add(int a, int b)
{
	int i;

	i = a + b;
	return (i);
}
