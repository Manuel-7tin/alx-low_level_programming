#include <stdio.h>
#include <stdlib.h>

/**
 * mod - modulos positive numbers.
 *
 * @a: first number
 * @b: second number
 *
 * Return: 0
 */

int mod(int a, int b)
{
	int i;

	i = a % b;
	return (i);
}
