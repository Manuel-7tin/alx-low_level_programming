#include <stdio.h>
#include <stdlib.h>

/**
 * mul - multiplies positive numbers.
 *
 * @a: first number
 * @b: second number
 *
 * Return: 0
 */

int mul(int a, int b)
{
	int i;

	i = a * b;
	return (i);
}
